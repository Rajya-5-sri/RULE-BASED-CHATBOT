# RULE-BASED-CHATBOT
A basic Python Commodity Price Chatbot that reads user input, detects simple intents (greeting, price query, goodbye), and can be extended to answer questions using a small in‑memory price list.
